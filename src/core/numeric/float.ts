export const FLOAT_CONSTANTS: Record<string, number> = {
  e: Math.E,
  pi: Math.PI,
  tau: Math.PI * 2,
  phi: (1 + Math.sqrt(5)) / 2,
  '\u03c0': Math.PI,
  '\u03c4': Math.PI * 2,
  '\u03d5': (1 + Math.sqrt(5)) / 2,
  '\u03c6': (1 + Math.sqrt(5)) / 2,
};
